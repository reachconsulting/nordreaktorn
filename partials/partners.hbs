{{#get "posts" filter="tag:partner" as |partners|}}
<div class="container py-5">
        <h1 class="text-center mb-5">Amazing Partners & Sponsorer</h1>
            <div class="row  justify-content-center">
        {{#foreach partners}}
        <div class="col-2">
            <img src="{{img_url feature_image}}" alt="{{title}}" class="img-fluid" />
        </div>
        {{/foreach}}
    </div>
</div>
{{else}}
{{/get}}

